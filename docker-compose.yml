services:
  whisprybot:
    build: .
    container_name: whisprybot
    restart: unless-stopped
    env_file:
      - stack.env
    volumes:
      - ./models:/app/.cache/huggingface
    working_dir: /app
